강제로 패치파일을 돌리기 위해서 만든 임시 py 파일들
1. merge_index_codes.py : 국내 지수값이 코드 자리수(4>6) 문제로 두개로 행이 생겨서 하나로 합치는 소스
2. recompute_std_last10.py : std 계산식 변경이 필요해서 우선 최근 10일치만 다시 계산해서 엑셀에 저장한 소스(만약 산식이 맞다면 전체 std 값 변경 필요)
